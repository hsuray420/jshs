import Link from "next/link";

export type SupportPage = Readonly<{ eyebrow: string; title: string; description: string; sections: readonly { title: string; body: string }[]; action?: { label: string; href: string } }>;
export function IndependentSupportPage({ page }: { page: SupportPage }) { return <><header className="jshs-hero-section"><div className="mx-auto w-[min(1120px,calc(100%-32px))] py-12 md:py-16"><p className="jshs-eyebrow">{page.eyebrow}</p><h1 className="mt-3 max-w-4xl">{page.title}</h1><p className="mt-4 max-w-3xl text-base leading-7 jshs-muted-copy">{page.description}</p></div></header><section className="mx-auto w-[min(1120px,calc(100%-32px))] pb-12"><div className="grid gap-4 md:grid-cols-2">{page.sections.map((section) => <article key={section.title} className="p-6 jshs-surface-card"><h2 className="text-xl">{section.title}</h2><p className="mt-3 text-sm leading-7 jshs-muted-copy">{section.body}</p></article>)}</div>{page.action ? <Link href={page.action.href} className="mt-6 inline-flex px-4 py-3 text-sm jshs-button-primary">{page.action.label} →</Link> : null}</section></>; }
